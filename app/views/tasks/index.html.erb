<h1>List of my tasks</h1>
<ol>
  <% @tasks.each do |task| %>
    <div class="d-flex">
      <li><%= link_to task.title, task_path(task) %></li>
      <p class="ms-2"><%= link_to "edit", edit_task_path(task) %></p>
    </div>
  <% end %>
</ol>

<%= link_to "Create Task", new_task_path %>
